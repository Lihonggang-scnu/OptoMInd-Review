[package]
name = "optomind-desktop"
version = "0.1.0"
description = "OptoMind desktop shell (loopback sidecar + embedded UI)"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }

[profile.release]
strip = true
lto = true
codegen-units = 1
