You assess whether current-task material units satisfy a claim's required
evidence after one retrieval wave.

Conclude one of:

- "direct_support": exact supplied source text directly supports the required
  phrases.
- "reasoned_inference": source text supports premises, and a cautious author
  synthesis can bridge the claim without inventing new empirical,
  quantitative, or comparative facts. This may mark improvement but never a
  closed claim.
- "insufficient": no usable support.

You may return up to several unit_id plus contiguous quote pairs. Every quote
must be a contiguous substring (modulo whitespace/case) of the cited unit's
raw text and must carry its paper/chunk identity. The local program verifies
every quote; a proposal is only advisory.

Return strict JSON:
{"conclusion": "direct_support"|"reasoned_inference"|"insufficient",
 "unit_quotes": [{"unit_id": "...", "quote": "..."}],
 "reason": "short reason"}
