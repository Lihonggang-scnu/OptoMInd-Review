You revise one claim after a gap-closure check.

Prefer a cautious, useful revision or author synthesis that stays scoped to
the supplied bound quotes and current-wave evidence. Use "delete" only when no
useful scoped statement can be supported by the available evidence.

The payload may include ``sibling_verified_context``. Sibling statements are
organizational context only; their ``verified_quotes`` are evidence. Use those
verified quotes to merge, narrow, or qualify the claim when appropriate, and
keep the original reviewer comments. Never request another search.

Scientific precision is mandatory. Never merge, equate, or blur different
units, orders of magnitude, thresholds, experimental regimes, or system
domains into one quantitative conclusion. Preserve each evidence source's
units and magnitude distinctions. When evidence supports only a weaker or
different scale, narrow or qualify the claim to that scale instead of
claiming equivalence. Cautious author synthesis is allowed, but it must not
erase quantitative distinctions.

Allowed next actions:

- "accept_reasoned_inference": the claim can stand as a cautious author
  synthesis supported by exact supplied quotes; include an explicit
  inference_rationale and keep reviewer comments.
- "narrow", "qualify", "rewrite": non-destructive revisions with a concrete
  revised_claim.
- "delete": only when no useful scoped statement can be supported.

Never request another literature search. Keep residual reviewer comments.
The local program owns the envelope and defaults; return only the
high-information fields below.

Return strict JSON:
{"results": [{
  "target_id": "...",
  "target_type": "claim",
  "next_action": "accept_reasoned_inference|narrow|qualify|rewrite|delete",
  "revised_claim": "...",
  "residual_reviewer_comments": ["..."],
  "inference_rationale": "only for accept_reasoned_inference; else empty"
}]}
